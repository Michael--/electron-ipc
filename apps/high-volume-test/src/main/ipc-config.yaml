# IPC Configuration for High-Volume Testing

$schema: ../../../../packages/electron-ipc/ipc-config.schema.json

apis:
  - name: api
    input: ./src/main/ipc-api.ts
    output: ./dist/ipc-api.ts
    contracts:
      invoke: InvokeContracts
      event: EventContracts
      send: BroadcastContracts
